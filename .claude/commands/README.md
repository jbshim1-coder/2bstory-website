# Claude Code 슬래시 커맨드

## 설치 방법

이 폴더 전체를 프로젝트 루트의 `.claude/commands/` 로 복사하세요.

```bash
# 프로젝트 루트에서 실행
mkdir -p .claude
cp -r [패키지위치]/03_바이브코딩_Claude_Code/.claude_commands .claude/commands
```

## 포함된 커맨드

- `/new-page` — 새 산업 페이지 생성
- `/check-content` — 미완성 항목 전체 점검
- `/deploy` — 빌드 검사 + 배포
